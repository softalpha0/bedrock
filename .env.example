# This is a template. Run:  cp .env.example .env
# then put your key in .env (which is git-ignored). NEVER put the key in THIS file.
# Get a key at https://coinmarketcap.com/api  — the hackathon grants Startup tier
# on your own key once you register on DoraHacks with your CMC account email.
CMC_API_KEY=

# Optional overrides
# PORT=8787
# CACHE_TTL_MS=60000
# CMC_BASE_URL=https://pro-api.coinmarketcap.com
